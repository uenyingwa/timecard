<nav class="navbar navbar-expand-sm navbar-color" style="background-color: #e3f2fd;">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <% if user_signed_in?  %>
          <div class="dropdown">
            <button class="btn btn-primary dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false" type="button">
              <%= current_user.full_name %>
            </button>
            <ul class="dropdown-menu">
              <li>
                <%= link_to 'Logout',  destroy_user_session_path, method: :delete, class: "dropdown-item" %>
              </li>
            </ul>
          </div>
        <% else %>
          <button class="btn me-2" type="button">
            <%= link_to 'Sign Up',  new_user_registration_path, class: 'nav-link' %>
          </button>
          <button class="btn me-2" type="button">
            <%= link_to 'Login',  new_user_session_path, class: 'nav-link' %>
          </button>
        <% end  %>
      </div>
    </div>
  </div>
</nav>
